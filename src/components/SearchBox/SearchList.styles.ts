import { Grid } from '@mui/material';
import styled from 'styled-components'


const SearchListStyles = styled(Grid)`
  padding-inline: .5rem;
  overflow: hidden;
  transition: all .25s ease-in;
`;

export default SearchListStyles; 